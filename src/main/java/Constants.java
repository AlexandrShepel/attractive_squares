/*
 * File: Constants.java
 * ---------------------
 * Contains the set of constants.
 */
public class Constants {

    /* The count of squares that user can interact with. */
    public static final int SQUARES_COUNT = 20;

    /* The size of each square. */
    public static final int SQUARES_SIZE = 10;

    /* The minimal interval between each square and window's border. */
    public static final int MINIMAL_DISTANCE_TO_BORDERS = 100;

    /* The higher the coefficient, the greater the attraction force. */
    public static final double ATTRACTION_FORCE_COEFFICIENT = 0.01;

    /* The maximum displacement of the square from the initial point. */
    public static final int MAXIMUM_DISPLACEMENT = 50;

    /* The pause between animation's frames. */
    public static final int PAUSE = 10;
}
